/**
 * Bump this when shipping homepage media/layout that must break sticky
 * browser/CDN caches for clients still holding an older shell or assets.
 * Used by /purge redirect query + DeployFreshness storage key lineage.
 */
export const HATHOR_SITE_CACHE_BUST = "20260811c";
